<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:padding="24dp">

    <!-- Título -->
    <TextView
        android:id="@+id/tvTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Assistente Fateb"
        android:textStyle="bold"
        android:textSize="22sp"
        android:textColor="#000000"
        android:layout_marginTop="16dp"/>

    <!-- Subtítulo -->
    <TextView
        android:id="@+id/tvSubtitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Assistência acadêmica instantânea ao seu alcance"
        android:textSize="15sp"
        android:textColor="#000000"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="16dp"/>

    <!-- Imagem central -->
    <ImageView
        android:id="@+id/ivIlustracao"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:src="@drawable/ilustracao_livros"
        android:contentDescription="Ilustração de estudante com livros"
        android:scaleType="fitCenter"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"/>

    <!-- Botão -->
    <Button
        android:id="@+id/btnComecar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Começar"
        android:textAllCaps="false"
        android:background="@drawable/botao_fundo"
        android:textColor="#000000"
        android:layout_marginBottom="16dp"/>

</LinearLayout>